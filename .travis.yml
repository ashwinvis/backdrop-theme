language: node_js
node_js:
  - "10"

before_install:
  - npm install -g grunt-cli
  - npm install -g bower

install:
  - npm install
  - bower install

cache:
  directories:
    - "node_modules"
    - "bower_components"

script: grunt

branches:
  only:
    - master
